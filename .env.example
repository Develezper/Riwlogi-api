OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Provider: "openai", "gemini", or "auto" (tries Gemini first, fallback to OpenAI)
AI_PROVIDER=auto

# Gemini
GEMINI_API_KEY=

# Optional per-feature model overrides
AI_CLASSIFY_MODEL=
AI_GENERATION_MODEL=

HOST=0.0.0.0
PORT=8001
CORS_ORIGINS=*
